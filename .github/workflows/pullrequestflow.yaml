name: PR Workflow
run-name: ${{ github.actor }} pull request
on:
  pull_request:
    types:
      - opened
    branches:
      - main
jobs:
  lint_and_test_pr_branch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
    #  - run: npm lint
    #  - run: npm test
